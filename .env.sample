# Microsoft Entra ID (Azure AD) – OpenID Connect settings
# Copy this file to `.env` and replace the placeholder values.

# Required: Your Entra ID Directory (tenant) ID
ENTRA_TENANT_ID=00000000-0000-0000-0000-000000000000

# Required: Your App registration's Application (client) ID
OPENID_CLIENT_ID=11111111-1111-1111-1111-111111111111

# Required: Redirect URI registered for the app (must match exactly)
# With WEBAPP_CONTEXT=ROOT, Guacamole is served at the root path.
OPENID_REDIRECT_URI=http://localhost:8080/
